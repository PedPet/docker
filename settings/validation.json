{
  "password": {
    "required": true,
    "length": {
      "min": 8,
      "max": 100
    },
    "regex": [
      "([A-Z])",
      "([^\\w\\d])",
      "([\\d])",
      "([a-z])"
    ],
    "format": "One uppercase character, one lowercase character, one number and a special character"
  }
}
